import { atom } from "recoil";

export const isShowSearchBarState = atom({
  key: "isShowSearchBarState",
  default: true,
});

export const isShowWebtoonState = atom({
  key: "isShowWebtoonState",
  default: false,
});

export const isShowCalendarState = atom({
  key: "isShowCalendarState",
  default: false,
});

export const isShowClockState = atom({
  key: "isShowClockState",
  default: false,
});

export const isShowDdayState = atom({
  key: "isShowDdayState",
  default: false,
});

export const isShowFileState = atom({
  key: "isShowFileState",
  default: false,
});

export const isShowVibeState = atom({
  key: "isShowVibeState",
  default: false,
});

export const isShowWeatherState = atom({
  key: "isShowWeatherState",
  default: false,
});

export const isShow1State = atom({
  key: "isShow1State",
  default: false,
});

export const isShow2State = atom({
  key: "isShow2State",
  default: false,
});

export const isShow3State = atom({
  key: "isShow3State",
  default: false,
});
